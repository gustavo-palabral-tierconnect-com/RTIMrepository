function condition(thingWrapper, things, messages, logger){

  var thingTypeCode = thingWrapper.getThingType().getCode();
  var Retail_SKU = thingWrapper.getUdf("Retail_SKU");
  if ((thingTypeCode == "PRODUCT") || (thingTypeCode.match('^PRODUCT_.*$'))) {
    //logger.warn(" ProductNormalization TRUE >> "+thingTypeCode);
    return true;
  } else {
    //logger.warn(" ProductNormalization FALSE >> "+thingTypeCode);
    return false;
  }
}